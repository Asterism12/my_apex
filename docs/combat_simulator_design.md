# Apex 3v3 战斗模拟器系统设计

## 一、系统概述

本系统用于模拟《Apex 英雄》风格的 3v3 团队战斗。用户可分别配置两支队伍在 **枪法**、**战斗经验**、**地形优势** 三个维度上的属性值。点击开始后，系统基于按tick执行的时间机制实时模拟战斗过程，逐阶段输出战斗日志，直至一方全灭或达到其他胜负条件。

---

## 二、核心模块划分

1. **队伍配置模块**  
   - 提供界面或接口，允许用户输入/调整两支队伍（Team A / Team B）的三项属性值（例如 0~100 的整数或浮点数）。  
   - 可支持预设模板（均衡型、突击型、防守型）或手动微调。  
   - 可选高级设置：队员个体差异、技能/装备配置（为后续扩展）。

2. **战斗模拟引擎**  
   - **初始化**：根据队伍属性创建 3 名队员实例，包含生命值、护甲、状态等。  
   - **Tick控制器**：决定模拟粒度（如每tick进行各类状态更新与动作判定）。  
   - **攻击计算器**：根据攻击方枪法、被攻击方战斗经验、地形优势（对双方影响）计算命中率、伤害值。  
   - **伤害与状态管理器**：处理血量变化、护甲损耗、击倒/淘汰状态，以及可能的恢复道具（可选）。  
   - **战术决策模块**（简化版）：在相应的tick判定中，根据当前局势自动选择攻击目标或使用技能（如集火、掩护、拉人）。  
   - **随机数生成器**：引入概率事件（爆头、失误、闪避），保证每局结果的可重复性或不可预测性（可设随机种子）。

3. **实时输出模块**  
   - 事件队列 → 按时间顺序生成文本/JSON 日志。  
   - 提供前端展示区域，逐行追加战斗过程（如“3.2s TeamA 队员1 击中 TeamB 队员3，造成 28 伤害”）。  
   - 支持暂停/继续、速度调节（×1, ×2, ×5）。  
   - 胜负判定后展示总结（MVP、总伤害、存活时间等）。

4. **胜负判定模块**  
   - 基本条件：某一队伍全部 3 名队员生命值 ≤ 0（淘汰）。  
   - 可选条件：时间限制（如 5 分钟内未分胜负则比较剩余总生命值或击杀数）。  
   - 触发后终止模拟引擎，显示胜者信息。

---

## 三、数据结构设计

### 3.1 队伍属性（全局参数）
| 属性 | 含义 | 影响范围 |
|------|------|----------|
| 枪法 | 射击精准度与暴击率 | 命中率、伤害浮动上限、爆头概率 |
| 战斗经验 | 战场应变与生存能力 | 闪避率、受击伤害减免、反制概率 |
| 地形优势 | 对地图环境的利用 | 局部掩体效果、先手概率、视野优势 |

> 每个属性的实际效果可映射为内部系数（0.5~1.5），例如枪法系数 = 属性值 / 50，范围 0~2。

### 3.2 队员实例属性（动态）
- 唯一标识 (ID)  
- 所属队伍  
- 当前生命值 (HP，上限 100~150)  
- 护甲值 (可选，抵挡部分伤害后破碎)  
- 状态: 存活 / 倒地 / 淘汰  
- 倒地倒计时 (被击倒后需队友救援，否则淘汰)  
- 攻击力基础值 (例如 15~25 随机浮动)  
- 技能使用标志 (简化：预设冷却时间，例如每 30 ticks 可使用一次战术技能)

### 3.3 全局战斗参数
- 模拟Tick时长 (例如 1 tick = 0.1 秒)  
- 攻击动作时间 (瞄准+射击约 8 ticks，换弹等由tick流逝自动处理)  
- 伤害公式:
  ```
  实际伤害 = 基础伤害 × 枪法系数(攻方) × (1 - 减伤系数(守方))
  命中率 = 基础命中率(0.7) × 枪法系数(攻方) / 闪避系数(守方)
  闪避系数 = 1 + 0.01×(战斗经验属性值)
  ```
- 爆头倍率：1.5~2.0，由枪法属性阈值触发

---

## 四、战斗模拟流程（按tick执行）

### 步骤 1 - 初始化
- 根据用户输入创建两队属性矩阵。  
- 生成 3 名队员：HP = 100，护甲 = 50（若启用）。  
- 计算队伍整体“先手值” = 地形优势平均值 + 随机扰动，决定初始tick哪一方获得行动优先权。  
- 清空输出缓冲区，启动模拟时钟 tick = 0。

### 步骤 2 - 主循环 (while 未结束)

#### 2.1 Tick更新
- `tick += 1`  
- 检查所有“倒地倒计时”队员，若倒计时归零则状态变为“淘汰”。  
- 触发冷却技能冷却时间递减。

#### 2.2 行动决策 (每 8~10 ticks允许一次攻击)
- 维护一个“行动就绪队列”，每个队员独立计算攻速（默认 10 ticks 攻击间隔）。  
- 当有队员就绪时，根据战术规则选择目标：
  - 优先攻击敌方**残血** (HP<30) 或**倒地**队员。  
  - 若无残血，则攻击敌方**当前输出最高**的队员（存活且护甲低）。  
  - 随机扰动模拟真实选择（+10% 随机偏离）。

#### 2.3 攻击结算
- 判定是否命中（基于命中率公式 + 随机数）。  
- 若命中，判定是否为爆头（枪法系数 > 1.2 且随机触发）。  
- 计算伤害并扣除目标 HP 或护甲。  
- 若目标 HP ≤ 0：
  - 状态变为“倒地”，记录倒地时间，触发“倒地倒计时”（默认 100 ticks）。  
  - 若全队均倒地或淘汰，则直接判定胜负。

#### 2.4 救援行动 (可选)
- 未攻击的队员可选择救援倒地队友，花费 30 ticks，期间无法攻击且易受攻击。  
- 救援成功后队友以 30% HP 重新站起。

#### 2.5 输出实时日志
- 每发生一次攻击、击倒、淘汰、救援事件，立即格式化输出到前端。  
- 事件附带时间戳、参与者、伤害值、剩余血量等关键信息。

### 步骤 3 - 胜负判定与结束
- 循环退出条件：
  - 某队存活队员数 = 0 → 宣布对方胜利。  
  - Tick超过预设上限 (如 3000 ticks) → 比较总剩余 HP 或击杀数。  
- 输出最终战报，包括每名队员的统计（造成伤害、承受伤害、击倒数）。

---

## 五、即时输出机制

- 使用**事件流**模式：模拟引擎在产生事件的tick时（命中、击倒等），立即推送到输出队列。  
- 前端轮询或 WebSocket 接收事件并动态追加到显示区域。  
- 为了可读性，对事件进行自然语言转换：
  - `[Tick 23] TeamA-猎犬 爆头击中 TeamB-恶灵 造成 58 伤害！ (恶灵剩余 HP: 12)`  
  - `[Tick 31] TeamB-恶灵 被击倒！ TeamB 剩余 2 人存活。`  
- 支持速度控制：实质上是调整每tick的实际等待时间或事件消费间隔。

---

## 六、属性影响细节及平衡性建议

| 属性 | 对攻击方影响 | 对防守方影响 |
|------|-------------|-------------|
| 枪法 | 提升命中率、爆头率、伤害系数 | - |
| 战斗经验 | 减少技能冷却（可选）、提升暴击抗性 | 提升闪避率、减少受击硬直、延长倒地倒计时 |
| 地形优势 | 增加隐蔽攻击概率、先手权 | 提高掩体减伤、降低被爆头概率 |

示例映射表（属性值 0~100）：  
- 枪法 20 → 命中率 -25%，伤害系数 0.8  
- 枪法 80 → 命中率 +20%，伤害系数 1.2  
- 战斗经验 90 → 闪避率 +18%，受击伤害减免 12%  
- 地形优势 70 → 先手概率 +35%，掩体减伤 15%

允许用户设置三属性总和限制（如 150 点），避免极端变态配置。

---

## 七、扩展与高级设计点

- **技能系统**：为每位队员增加一个独特战术技能（例如“烟雾弹”降低敌方命中率，“治疗无人机”缓慢回血）。  
- **完整比赛模拟可视化**：生成时间轴图表，按tick展示血量变化曲线。  
- **概率分布记录**：为开发者提供随机性验证工具，确保模拟结果不过分依赖运气。  
- **多人同步模拟**：支持同一房间多个观看者实时观看战斗过程。  
- **历史回放**：保存战斗事件流，可回放重演。

---

## 八、总结

该设计提供了一个模块化、可扩展的 3v3 战斗模拟器框架。核心是**队伍属性影响战斗公式**，配合**按tick执行 + 事件驱动**的模拟引擎，可实时输出详细战况，直至分出胜负。无需编写代码即可理解数据流转与交互逻辑，方便后续实现或调整规则。