# 团队沟通日志系统（Team Comms Log）设计文档

## 一、 系统概述
团队沟通日志系统旨在为《Apex 大逃杀 20队模拟》增加沉浸感与角色扮演体验。在后台的宏观或微观 Tick 运转时，系统会基于队伍当前的状态（搜集、跑毒、接战、倒地、夺冠）结合预置的语料库，随机抓取文本并组成战队内部的“语音小队频道（Voice Comms）”。
在本局模拟结束后，系统提供UI界面供玩家回放和审阅某一支队伍从跳伞到结束（或阵亡）全过程的内部沟通记录。

## 二、 沟通事件分类与触发时机（Events）

与游戏底层 tick 的逻辑做事件绑定，沟通的触发契机细分为以下几类：

1. **游戏开局 (DROP)**
   - 触发时机：`Tick == 0`，队伍初始化。
   - 包含内容：落点决策、互相打气。
2. **物资搜集 (LOOT)**
   - 触发时机：队伍处于 `status === 'loot'` 且在安全区内。
   - 包含内容：报点物资、寻求弹药、护甲升级。
3. **跑毒进圈 (RING_MOVE)**
   - 触发时机：`status === 'move'` 或 受到真实毒圈伤害时。
   - 包含内容：催促队友、抱怨运气差。
4. **遇敌/开战接敌 (ENGAGE_ALERT & ENGAGE_INFO)**
   - 触发时机：`status` 从非 `fight` 切入 `fight` 时。分为两步报点：
     - `ENGAGE_ALERT`: 在触发交火的当前 Tick，进行遇敌预警。
     - `ENGAGE_INFO`: 在触发交火后的下一 Tick (`tick + 1`)，精确报出敌方身份。
   - 包含内容：受击预警、集火指令、报出敌方队伍名称。
5. **战术反馈：击倒/受击 (COMBAT_FEEDBACK)**
   - 触发时机：某队员造成有效击倒，或自己被击倒。
   - 包含内容：大喊“击倒一个”、“残血、集火他！”、“我倒了，能拉/快跑”。

6. **战斗微观状态反馈 (COMBAT_MICRO)**
   - 触发时机：微观战斗引擎 v2 产生的各类战斗状态变化时。
   - 细分为以下子类：
     - **破甲预警 (SHIELD_BREAK)**：
       - 触发时机：己方队员护盾归零（破甲）时。
       - 包含内容：警告队友自己没甲了、提醒队友对面破甲可以压。
     - **换弹通报 (RELOAD)**：
       - 触发时机：己方队员弹匣打空进入换弹状态时。
       - 包含内容：提醒队友自己在换弹无法输出、掩护我。
     - **打电池/医疗包 (HEALING)**：
       - 触发时机：队员缩回掩体开始打电池（`healing_shield`）或打医疗包（`healing_hp`）时。
       - 包含内容：告知队友自己在回状态、请求架枪掩护。
     - **被压制 (SUPPRESSION)**：
       - 触发时机：队员被集火命中 ≥ 2 发，强制进入 `in_cover` 状态时。
       - 包含内容：被锁了不敢探头、请求队友反架。
     - **救援相关 (REVIVE)**：
       - `REVIVE_START`：开始救援倒地队友时，告知队友在拉人。
       - `REVIVE_SUCCESS`：救援成功，鼓励队友或提醒先打电池。
       - `REVIVE_CANCEL`：救援被打断，提醒队友情况危急。
     - **补人 (EXECUTION)**：
       - 触发时机：攻击方选择补掉倒地敌人时。
       - 包含内容：大喊“我补了他！”、“对面减员，压过去！”。
     - **爆头反馈 (HEADSHOT)**：
       - 触发时机：对敌人造成爆头伤害时。
       - 包含内容：兴奋报点“爆头了！大残！”。

7. **第三方介入/劝架 (THIRD_PARTY)**
   - 触发时机：队伍以听见枪响加入混战，或者在交战时检测到新的第三方队伍加入战斗 `Combat Instance`。
   - 包含内容：“有黄雀！先拉扯！”、“他们打起来了，我们去当黄雀！”
7. **胜负终局 (GAME_OVER)**
   - 触发时机：全员遭到淘汰（`status === 'dead'`），或成为最终捍卫者。
   - 包含内容：遗憾道歉、互相甩锅、或者夺冠的狂欢。

## 三、 语料数据字典结构（Data Corpus）

采用参数占位符（如 `{队友}`, `{敌方}`）与随机语料数组来实现多变性。

```javascript
const COMM_DICTIONARY = {
    "ENGAGE_ALERT": [
        "脸上有一队！准备打！",
        "发现敌人！集火集火！"
    ],
    "ENGAGE_INFO": [
        "看前面，对面是 {敌方}！",
        "是 {敌方} 的人，优先处理他们！"
    ],
    "COMBAT_MEMBER_DOWN": [
        "我被 {敌方} 击倒了！不要管我！",
        "哎呀大残！拉我一下！"
    ],
    "COMBAT_ENEMY_DOWN": [
        "NICE！击倒 {敌方} 一个！压过去！",
        "{敌方} 掉了一个！快上！"
    ],
    "THIRD_PARTY_INCOMING": [
        "我靠，又来一队！情况不对先撤！",
        "是 {敌方} 来劝架了，打大电！"
    ]
    // ... 其他分类
}
```

## 四、 架构与实现方案（三层架构适配）

1. **逻辑层 (Logic Layer) - `engine.js`**
   - **状态扩张**：在初始化的每个 Team 对象内增加 `comms: []` 的数组，用来按序存储该队伍的沟通流水。格式如：`{ tick: 120, speaker: "ImperialHal", text: "快跑毒！" }`
   - **拦截挂载**：在 `processBRTick` 内，当执行到对应判断时（例如扣血、进入 Combat），调用辅助函数 `addComm(team, event, params)`，按极小概率触发随机对话推入该队伍的 `comms` 数组中（防止日志量过大，设置一定冷却机制）。
   
2. **控制层 (Control Layer) - `localAdapter.js`**
   - 接口无需变动，队伍状态下的 `comms` 数组会随着原版 `State` 直接返回到前端内存中。

2. **界面层 (UI Layer) - `app.js` & HTML**
   - **观战跟随模式 (Live Spectate)**：在模拟过程中，增加“队伍跟随选择”下拉列表（可实时切换），选定队伍后展示专门的“团队信息面板”，实时滚动该队伍的状态更新与沟通日志。
   - **实时语音播报 (Real-time TTS)**：面板旁增加一个“播放通讯语音”复选框（默认勾选）。如果开启且有新通讯日志出现，通过 TTS 进行朗读。
   - **赛后复盘UI**：在游戏结束（`status === 'end'`）时，在页面右侧或弹窗弹出 **「赛后小队沟通回看」** 面板。
   - **交互**：提供下拉菜单（`<select>`）让用户赛后查看全量沟通记录。
   - **渲染**：类似微信聊天框的模拟（带发言人名字、Tick 时间戳和聊天气泡）。

## 五、 输出样例（Example）

假设选择了 TSM 战队，最终的界面可能呈现如下的滚动日志：

```text
[赛后沟通记录查看] - 队伍：TSM (当前状态：淘汰)

[Tick 0] ImperialHal: 我们跳碎片东部，跟着我！
[Tick 45] Reps: 这边有紫甲和电冲，我帮你标了。
[Tick 120] Verhulst: 圈太远了，别搜了赶紧走兄弟们。
[Tick 185] ImperialHal: 脸上有一队！是 NRG ，准备打！
[Tick 190] Verhulst: NICE！击倒 NRG 一个！压过去！
[Tick 195] Reps: 我靠，又来一队！XSET来劝架了，拉扯一下！
[Tick 198] ImperialHal: 我被击倒了！不要管我！
[Tick 200] Reps: 没救了，我的我的。 (全队淘汰)
```

## 六、 总结
这套系统的引入完全独立于战斗结算核心算法，仅作为附属观察属性存在。它通过利用已有的三层架构中逻辑层的状态存储机制，实现零网络开销、纯本地化的赛事回放体验，并且能很好地兼容已有的 `teams.js` 真实选手姓名数据库。