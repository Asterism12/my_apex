# Apex 世界赛决赛模拟系统设计 (Match Point 赛制)

## 一、系统概述
基于现有的「Apex 20队大逃杀模拟系统」，将其从“单局模拟”扩展为**多局连续模拟的赛事总控系统**。本系统引入 ALGS (Apex Legends Global Series) 经典的**赛点制 (Match Point Format)**。参赛的 20 支队伍通过在多局比赛中积累“击杀分”和“排名分”，当某支队伍达到 **50分** 的阈值后，即在下一局开启“赛点”（Match Point Eligible）。已开启赛点的队伍若在后续的任一局中成功“吃鸡”（成为捍卫者），则直接夺得赛事最终总冠军。

## 二、核心规则与模块扩展

### 1. 赛事积分模块 (Scoring System)
单局内所有行为转化为标准的赛事积分：
- **击杀分**：每次成功淘汰一名敌方玩家，队伍获得 **1 分**。
- **排名分**：单局结束后，根据队伍的最终排名分发：
  - 第 1 名：12 分
  - 第 2 名：9 分
  - 第 3 名：7 分
  - 第 4 名：5 分
  - 第 5 名：4 分
  - 第 6-7 名：3 分
  - 第 8-10 名：2 分
  - 第 11-15 名：1 分
  - 第 16-20 名：0 分

### 2. 多局赛事循环引擎 (Tournament Controller)
- **跨局状态继承**：一局结束后，清理单局内的战场状态（生命值、装备、物资、坐标），但**持久化保存**各队伍的参赛积分和赛点状态。
- **下一局初始化**：保留赛事总进度（如进入 Match 3），重新分配 20 支队伍的随机跳伞落地航线及初生物资。

### 3. 赛点判定机制 (Match Point Logic)
- **触发阈值**：总积分 $\ge$ 50。
- **更新时机**：队伍状态只在**两局比赛的过渡阶段**结算并生效。如果在战局进行中临时突破 50 分，当前局也不能直接夺冠，须等下一局才能正式处于“赛点开启”状态。
- **夺冠条件**：`队伍包含赛点状态 (IsMatchPointEligible == true)` **且** `荣名单局第一 (Placement == 1)`。

## 三、数据结构设计补充

### 3.1 赛事总控级别 (Tournament State)
```json
{
  "matchCount": 1,           // 当前对局场次
  "tournamentStatus": "IN_PROGRESS", // 赛事状态 (IN_PROGRESS, FINISHED)
  "championTeamId": null,    // 冠军队伍ID
  "eligibleTeams": []        // 当前已开启赛点队伍列表
}
```

### 3.2 队伍赛事属性扩展 (Team Tournament Properties)
在单局原有属性基础上，新增赛事级属性：
- **TotalScore**: 当前累积总积分 (Kills + Placement)。
- **TotalKills**: 当前累积总击杀数。
- **MatchesPlayed**: 参与的对局总数。
- **IsMatchPointEligible**: 布尔值，是否已达 50 分开启赛点。
- **DynamicAggressiveness**: 动态激进度。系统将根据队伍的当前分数动态调整其在局内的策略。
  - **赛点防守型**：已达 50 分的队伍，其 AI 激进度下调，不主动劝架，策略改为“避战进圈”和“占据房区”。
  - **追分激进型**：未达 50 且落后较多的队伍，激进度上调，优先寻找枪声拿击杀分。

## 四、决赛模拟流程

### 阶段 1：赛事初始化
- 加载 `teams.js` 中的 20 支队伍配置。
- 所有队伍的 `TotalScore` 归 0，`IsMatchPointEligible` 设为 `false`。
- 设定当前局次 `MatchCount = 1`，赛事正式启动。

### 阶段 2：单局大逃杀模拟
- 启动 **「大逃杀宏观与微观战斗模拟引擎」** (复用 `engine.js` 扩展功能)。
- 战局内部照常处理 Tick 更新：跳伞、刷圈、资源发育与局部 3v3(多队) 混战。
- 战斗中产生的人员淘汰直接缓存计入 `RoundKills`。
- 对战决出最后一支存活队伍，单局游戏结束。

### 阶段 3：单局结算与赛点检定
1. **分数核算**：结算这 20 支队伍的单局得分 `$RoundScore = RoundKills \times 1 + PlacementScore$`。
2. **累加总分**：`$TotalScore += RoundScore$`。
3. **夺冠判定**：检查本局的第一名队伍（捍卫者）。若该队的 `IsMatchPointEligible` 为 `true`，则直接判定**赛事结束，该队伍夺冠**。进入阶段 4。
4. **赛点刷新**：如果未产生冠军，则遍历所有队伍。将当前 `$TotalScore \ge 50$` 的队伍的 `IsMatchPointEligible` 标记为 `true`。
5. **准备新局**：`MatchCount + 1`，重置所有队伍的场内数据，回到阶段 2 开始下一局。

### 阶段 4：赛事落幕与总战报打点
- 比赛结束。生成**《APEX世界赛决赛总排行榜》**。
- **最终排名排序规则**：
  1. 夺得总冠军的球队绝对排在第 1 名。
  2. 其余队伍按照 `TotalScore` 降序排列。
  3. 若积分相同 (Tie-breaker)：优先比较“总赛点局数”，其次比较“单局最高得分”，再其次比较“总击杀数”。
- 输出完整的对局历史 JSON 及可视化面板所需数据。

## 五、可视化及日志补充要求

1. **赛点高亮通知面板 (Match Point Alerts)**
   - 在前端界面的积分板（Leaderboard）上，达到 50 分的队伍增加特殊金色火焰图标 (🔥 Match Point)。
   - 界面置顶显示：`"赛点队伍数量：3 | Match: 7"`。
   
2. **决赛特有战局广播 (Championship Broadcast)**
   - 包含赛点队伍时的缩圈广播：`[全局播报] 决赛圈剩余 3 队，其中 Team_TSM 为赛点队伍，冠军有可能即将诞生！`
   - 赛点队伍被淘汰高亮广播：`[全局播报] Team_TSM (赛点队伍) 已被淘汰！系统进入下一局的概率增加！`
   - 夺冠时刻全屏结算：`[最终结算] TEAM_TSM 成功捍卫并斩获冠军！经历 7 局鏖战，比赛正式落幕！`
